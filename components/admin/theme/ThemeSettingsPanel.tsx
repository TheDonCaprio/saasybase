"use client";

export default function ThemeSettingsPanel(): JSX.Element | null {
  // Deprecated: ThemeSettingsPanel was replaced by ThemeSettingsTabs.
  // Keep a minimal no-op stub so any leftover imports don't break builds.
  return null;
}
