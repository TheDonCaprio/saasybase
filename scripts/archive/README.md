/*
Archived scripts

This folder contains archived development and demo scripts moved from the top-level
`pro-app/scripts/` directory. These files are preserved for history and occasional
manual use. They are excluded from TypeScript compilation by `pro-app/tsconfig.json`.

If you need to restore a script to active use, copy it back to `pro-app/scripts/` and
remove the header comment that marks it archived.
*/
